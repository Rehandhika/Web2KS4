---
const dnaItems = [
  {
    title: "TECHNICAL PRECISION",
    id: "01",
    description: "Every line of code, every design choice is made with surgical accuracy.",
    class: "md:col-span-2 bg-midnight-800"
  },
  {
    title: "COLLECTIVE",
    id: "02",
    description: "Strength in numbers, united by a single vision of excellence.",
    class: "md:col-span-1 bg-midnight-800"
  },
  {
    title: "EVOLUTION",
    id: "03",
    description: "Constant iteration. We don't just adapt, we redefine the standard.",
    class: "md:col-span-1 bg-midnight-800"
  },
  {
    title: "INDUSTRIAL SOUL",
    id: "04",
    description: "A class culture built on the foundations of hard work and structural integrity.",
    class: "md:col-span-2 bg-azure text-midnight-900"
  }
];
---

<section id="class-dna" class="section-padding bg-midnight-900 min-h-screen flex items-center justify-center">
  <div class="container-custom w-full">
    <div class="flex flex-col md:flex-row justify-between items-end mb-24 gap-8">
      <div class="max-w-2xl">
        <div class="font-mono text-xs text-azure tracking-[0.4em] uppercase mb-6">// CORE_PRINCIPLES</div>
        <h2 class="font-display font-bold text-5xl md:text-7xl text-white leading-none">STRUCTURAL<br/><span class="text-azure">DNA</span></h2>
      </div>
      <div class="max-w-sm">
        <p class="text-mist/40 font-light text-sm leading-relaxed">Defining the parameters of excellence through a modular approach to collaboration and innovation.</p>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-1">
      {dnaItems.map((item) => (
        <div class={`bento-card group relative overflow-hidden flex flex-col justify-between min-h-[300px] ${item.class}`}>
          <div class="font-mono text-[10px] opacity-40">{item.id}</div>
          <div>
            <h3 class="font-display font-bold text-2xl mb-4 tracking-tight">{item.title}</h3>
            <p class="text-sm opacity-60 font-light leading-relaxed">{item.description}</p>
          </div>
          <div class="absolute top-0 right-0 w-24 h-24 border-t border-r border-white/5 group-hover:border-azure/40 transition-colors"></div>
        </div>
      ))}
    </div>
  </div>
</section>
